# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Logs
*.log
npm-debug.log*

# Node (only relevant if you ever add a build step)
node_modules/

# Jekyll build output (disabled via .nojekyll, but ignore just in case)
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
